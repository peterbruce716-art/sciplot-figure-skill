# Layout Archetype Examples

These are lightweight, deterministic planning examples for the four FigureContract archetypes. Each directory contains a contract, a chart-decision sketch, a VisualSpec v2-compatible layout sketch, and an expected manifest fragment. They intentionally do not bundle rendered previews or source data; release acceptance remains the strict rendered example gate.
