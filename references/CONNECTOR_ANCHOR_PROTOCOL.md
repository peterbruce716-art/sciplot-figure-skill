# Connector anchor protocol

Connectors reference element IDs and semantic sides (`top`, `right`, `bottom`, `left`, or `center`) rather than storing only final pixel coordinates. The audit resolves anchors on the geometry skeleton and reports dangling references, out-of-bounds endpoints, or invalid side names.
