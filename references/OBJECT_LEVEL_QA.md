# Object-level QA

Object-level QA creates an ID map and binary masks, scores corresponding regions on a fixed canvas, and maps connected diff components back to object IDs. A whole-image score remains useful, but it is not sufficient to hide one failed annotation inside a good average.
