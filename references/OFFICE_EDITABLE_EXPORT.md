# Office-compatible editable export

`export_office_vba.py` emits a complete VBA source skeleton with normalized coordinate conversion, stable shape names, text boxes, preserved image insertion, and connector creation. It is optional and independent of the Python reconstruction path. The report distinguishes generated source from runtime verification; no runtime claim is made without an actual host verification.
